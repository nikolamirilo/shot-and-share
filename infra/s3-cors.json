{
  "CORSRules": [
    {
      "_comment": "Guests POST directly to the bucket with a presigned policy. Replace the origins with the real hostnames before going live — a wildcard here is an invitation to have your upload policy used from somebody else's page.",
      "AllowedOrigins": [
        "https://saycheese.app",
        "https://www.saycheese.app",
        "http://localhost:3000"
      ],
      "AllowedMethods": ["POST", "GET", "HEAD"],
      "AllowedHeaders": ["*"],
      "ExposeHeaders": ["ETag", "Location"],
      "MaxAgeSeconds": 3000
    }
  ]
}
